/* Defensive Positioning Assistant — App Entry Point
 * Initializes the application after all modules are loaded.
 * Must be the last script loaded.
 */

load(); rotateVerse(); setupDoubleTap(); render();
