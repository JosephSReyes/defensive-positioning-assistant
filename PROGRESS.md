# Defensive Positioning Assistant — Project Progress

## Current Status

**Current Step:** Step 13 complete (Play-Log Import) — Step 12 (full regression suite) still open  
**Phase:** Feature delivery / Final Validation  
**Status:** Step 13 Complete

---

## How to Use This File

- Update `Current Step` and `Status` before starting each session.
- Mark steps complete only after all checklist items and tests pass.
- Add notes under each step if anything deviated from the playbook.

---

## Step Completion Log

### Phase 1 — Repository Stabilization

- [x] **Step 1** — Initialize GitHub Repository and Preserve Original Prototype
  - Commit: `chore: initialize repository and preserve original prototype` (root commit `b246c00`, pushed to `origin/main`)
  - Notes: Original prototype preserved byte-for-byte as a `legacy/` snapshot (renamed from a doubled-extension filename; MD5 verified identical). `index.html` created at repo root as the working copy (MD5 identical to the legacy snapshot). The legacy snapshot is not part of this published tree. `README.md` and a Netlify/JS `.gitignore` added. No `package.json` added — not required until Step 2 testing. `git init`/`commit`/`push` were run manually on Windows because the agent sandbox could not reliably host a `.git` directory in the mounted folder. Smoke check passed: HTTP 200, field UI markup present, no fatal JS syntax errors in any inline script block.

- [x] **Step 2** — Set Up Testing Infrastructure
  - Commit: `test: add vitest and playwright testing infrastructure`
  - Notes: Added `package.json` (vitest ^2.0.0, @playwright/test ^1.44.0, serve ^14.2.3). Created `vitest.config.js` and `playwright.config.js`. Created `tests/unit/app.test.js` (8 unit smoke tests) and `tests/e2e/smoke.spec.js` (6 E2E smoke tests). All tests verified passing — 8/8 unit, 6/6 E2E. Vitest must be ^2.0.0 for Node 22 compatibility; do not downgrade. Playwright E2E tests use `npx serve` on port 3000 (auto-started by Playwright config). Run `npx playwright install chromium` before first E2E run.

- [x] **Step 3** — Extract CSS Into Separate File
  - Commit: `refactor: extract inline css into external stylesheet`
  - Notes: Extracted two inline `<style>` blocks (lines 25–1955 and 4945–5009 in the original `index.html`) into `styles/main.css` (1998 lines, 448 CSS rule blocks). Replaced both blocks with a single `<link rel="stylesheet" href="styles/main.css">` at the original position of the first block, inside `<head>`. No JavaScript or HTML structure was modified. Added 6 CSS regression tests to `tests/unit/app.test.js` (Step 3 suite) and 2 E2E regression tests to `tests/e2e/smoke.spec.js` (stylesheet HTTP 200 + computed background-color check). All 15 unit tests pass (8 original + 6 new CSS + 1 minimal). Note: avoid literal `<style` in `.test.js` source — Vite's `import-analysis` plugin misidentifies such files as HTML and refuses to parse them; use `new RegExp('<' + 'style...')` instead.

- [x] **Step 4** — Extract JavaScript Into Modular Files
  - Commit: `refactor: modularize embedded javascript`
  - Notes: Extracted all 12 inline `<script>` blocks from `index.html` into 10 module files under `js/`: `storage.js`, `utils.js`, `game.js`, `recommendations.js`, `ui.js`, `roster-import.js`, `memberstack.js`, `batter.js`, `spray-charts.js`, `app.js`. Pitcher management blocks (DefensivePositioningMyPitchers, DefensivePositioningPitcherMenu, DefensivePositioningPitcherRepair) and Live Pitch Intelligence were excluded because pitchers were ruled out of scope for the app. Files `js/pitchers.js` and `js/pitch-intelligence.js` were created then excluded — they remain on disk but are not referenced by `index.html` and cannot be deleted due to sandbox permissions; they must be removed manually. Load order in HTML is dependency-safe: storage → utils → game → recommendations → ui → roster-import → memberstack → batter → spray-charts → app. `batter.js` (compact batter display) patches `renderGame` and loads after `ui.js`. All 10 modules pass Node syntax check. 73/73 unit tests pass (59 new Step 4 tests + 14 prior). All 10 module files serve HTTP 200. E2E Playwright tests written in `tests/e2e/modules.spec.js` — run `npm run test:e2e` locally to verify (sandbox bash timeout prevents agent-side E2E execution).

### Phase 2 — Netlify + Environment Configuration

- [x] **Step 5** — Add Netlify Configuration and Environment Variables
  - Commit: `chore: add netlify configuration and environment variable support`
  - Notes: Created `netlify.toml` (publish dir `.`, functions dir `netlify/functions`, API redirect `/api/*` → `/.netlify/functions/:splat`, security headers). Created `.env.example` with placeholders for `OPENAI_API_KEY`, `STRIPE_SECRET_KEY`, and `MEMBERSTACK_PUBLIC_KEY` — no real secrets. Created `netlify/functions/` with three stub files: `openai-roster-import.js`, `ai-coach-brief.js`, `stripe-webhook.js` — each exports a handler returning 501 until implemented in later steps. Added `tests/integration/env.integration.test.js` with 23 tests covering: .env.example keys, netlify.toml structure, functions directory and stub existence, handler exports, and no-hardcoded-secrets checks in all frontend JS files. All 96 tests pass (73 prior + 23 new). Integration test file uses `.integration.test.js` suffix to match vitest.config.js glob pattern.

### Phase 3 — Memberstack Authentication

- [x] **Step 6** — Integrate Memberstack Authentication
  - Commit: `feat: integrate memberstack authentication`
  - Notes: Added Memberstack DOM SDK script tag (`data-memberstack-id="app_cmp1m2fbx008o0sye89c483i1"`) to `<head>`. Removed test-mode bypass — `landingScreen` is now the default active screen. Updated `js/memberstack.js` with: login form submit handler (`loginMemberEmailPassword`), signup form submit handler (`signupMemberEmailPassword` with plan `pln_pro-7y00gqr`), `dpaLogout()` function, and a `showScreen` auth-guard wrapper that redirects unauthenticated users to `landingScreen` for protected screens (homeScreen, teamScreen, gameScreen). Added logout button (`#dpaLogoutBtn`) to `homeScreen .home-links`. Session persistence handled by `goToAppIfLoggedIn` which auto-redirects on load. Secret key never exposed to frontend. Added 27 integration tests + 8 E2E tests. All 123 tests pass.

### Phase 4 — Stripe Billing

- [x] **Step 7** — Integrate Stripe Checkout (via Memberstack native billing)
  - Commit: `feat: integrate stripe subscription checkout`
  - Notes: Stripe billing is handled entirely through Memberstack's native Stripe connection (`acct_REDACTED`). No standalone Stripe Netlify Functions are required. Architecture: Memberstack owns Auth + Billing; Netlify owns Hosting + AI Functions (Steps 8-9). Change from initial implementation: restored `plans: [{ planId: DPA_PLAN_ID }]` in `signupMemberEmailPassword` call (deferred in Step 6 until Stripe was confirmed connected). Memberstack handles the Stripe checkout redirect and subscription lifecycle internally. Removed `netlify/functions/stripe-create-checkout.js`, `netlify/functions/stripe-webhook.js`, and `js/stripe.js` — all unnecessary since Memberstack is the billing layer. Removed `stripe` npm dependency. Removed Stripe env vars from `.env.example` (only `OPENAI_API_KEY` and `MEMBERSTACK_PUBLIC_KEY` documented). Updated Step 5 env integration tests to reflect removal of stripe-webhook.js stub. Added 24 integration tests + 9 E2E tests. All 186 tests pass (145 unit/integration + 41 E2E). No Stripe API keys are needed in Netlify environment variables — Memberstack manages this via its dashboard connection.

### Phase 5 — OpenAI Roster Import

- [x] **Step 8** — Implement AI Roster Import
  - Commit: `feat: add ai-powered roster import`
  - Notes: Replaced Tesseract-based `imageRosterNotice` in `js/roster-import.js` with an OpenAI Vision call via the Netlify Function `netlify/functions/openai-roster-import.js`. Frontend reads image as base64 via FileReader, POSTs `{ image, mimeType }` to `/api/openai-roster-import`, and displays extracted players in the textarea for review before import. Function uses `gpt-4o` with `detail: low` for cost efficiency, validates inputs, strips HTML chars from names, and keeps only digits in jersey numbers. Error and loading states handled. 18 integration tests + 4 E2E tests added. All 209 tests pass (163 unit/integration + 46 E2E). No API keys exposed to frontend.

### Phase 6 — AI Coach Brief

- [x] **Step 9** — Implement AI Coach Brief Explanation
  - Commit: `feat: add ai-generated coach brief explanations`
  - Notes: Implemented `netlify/functions/ai-coach-brief.js` using OpenAI `gpt-4o-mini`. Created `js/ai.js` with `fetchAICoachBrief()` and `buildAIBriefContext()` — caches up to 5 responses in localStorage with a 1-hour TTL. 28 integration tests + 19 unit tests added. 6 E2E tests added. All 213 vitest tests pass. No API keys exposed to frontend.
  - Post-commit refactor (same session, separate commit `f81bef0`): Removed SOS Situational Adjustment button and all related code (`openSituationAI`, `applySituationToDefense`, `renderSituationControls`, modal HTML, situation CSS). Redesigned `#aiExplainBox` as a full-width bottom sheet (was a small fixed top overlay). New two-section layout: rules-based analysis text shown immediately on 🤖 AI tap; separate "Ask AI to explain this read" button triggers the OpenAI fetch on demand. `buildAIBriefContext()` now includes current fielder move recommendations and the local analysis text so the AI can see and explain exactly what the rules-based engine decided. `toggleAIExplain()` no longer auto-fetches AI; `requestAICoachBrief()` is the new on-demand handler. Updated tests to reflect removed situation functions and new behavior.
  - UI refinement (separate commit): Removed manual "Ask AI" and "×" close buttons. Tapping the 🤖 AI button now immediately fires the AI fetch. Panel restructured as a flexbox column — scrollable `.ai-explain-content` area above a sticky `.ai-explain-footer` containing a full-width gold "Got it" button (large, thumb-friendly). `max-height` raised to 85vh for more vertical space. `toggleAIExplain()` auto-calls `requestAICoachBrief()` on open; `requestAICoachBrief()` sets loading text directly in `#aiCoachBriefText` (no button state to manage). E2E close test updated to target `.ai-got-it-btn`. All 213 tests pass.

### Phase 7 — UI Improvements

- [x] **Step 10** — Implement Mobile Layout Improvements
  - Commit: `feat: improve mobile single-screen experience`
  - Notes: Multiple iterative refinements across `index.html`, `js/batter.js`, `js/game.js`, `js/recommendations.js`, and `styles/main.css`. Key changes: (1) **Batter strip chips** — `playerLabel()` restructured to emit separate `hand` and `rosterSpot` fields; compact batter display replaced the plain grey subtext line with color-coded chips (RH=blue, LH=orange, SH=purple) via `.dpa-batter-chip` CSS classes. (2) **End Game confirmation modal** — replaced the native browser `confirm()` dialog with a styled in-app modal matching the AI Coach popup pattern; added `openEndGameModal()`, `closeEndGameModal()`, `confirmEndGame()` to `game.js`; red destructive theme with Cancel (gold, visually dominant) and End Game (red outline, deemphasized). (3) **AI Coach popup redesign** — changed from gold-accent bottom sheet to vertically-centered green-accent popup; replaced ⚾ emoji avatar with an SVG shield icon; recolored from gold `#f4c542` to green `#40b03e` throughout (border, glow, "Got it" button). (4) **Current mode tab** — changed active tab color from app-red `#e91a28` to gold `#f4c542` so red is reserved exclusively for destructive actions. (5) **Hot/cold pill fix** — moved slump/hot-streak detection earlier in `renderSmartFielding()` so pills display even before any field contacts are logged; broadened hot streak to include "Single"; tightened sample window to 2 ABs. (6) **Removed swipe buttons** — legacy `.swipe.left` / `.swipe.right` overlay buttons removed from HTML and CSS; navigation is handled by the compact batter strip chevrons. (7) **Outcome modal polish** — `outcome-box` border, background, and button styles tightened to match the rest of the game screen card vocabulary. (8) **Mobile tag font-size** bumped 10px→12px for readability. (9) **Consolidated CSS section** added at end of `main.css` for all new game screen chip and modal rules. Added 4 unit tests verifying the new end-game modal functions and confirming `endGame()` no longer calls native `confirm()`. All prior tests continue to pass.
  - **Post-Step-11 follow-up — Player perspective + swipe gesture (closes contract items 6 & 7):**
    - `js/recommendations.js`: flipped `left` ↔ `right` in the `setBaseRecs` branches so movement directions are stated from the fielder's own perspective (facing home plate). Right Side pull now outputs "left" for RF/2B/CF; Left Side pull outputs "right" for LF/SS/CF. Middle/back/Hold bag/Guard line/Straight up/Stay honest are perspective-neutral and unchanged.
    - `netlify/functions/ai-coach-brief.js`: extended `SYSTEM_PROMPT` so the OpenAI explanation also uses the fielder's own perspective and stays consistent with the on-field tags.
    - `js/batter.js`: added `setupSwipeNavigation()` — touchstart/touchend handler on `#gameScreen .field-wrap` with 50px horizontal / 50px vertical / 600ms thresholds. **Swipe left → `nextPlayer()`** (advance to next batter); **swipe right → `prevPlayer()`** (go back). Matches the standard carousel convention so the swipe direction tracks lineup order (settled design choice). Ignores swipes that originate on `button`/`a`/`input`/`textarea`/`select` so chevron taps still work. Uses `{ passive: true }` listeners.
    - **Baseline tests updated to match the current landing-page architecture** — `tests/unit/app.test.js`, `tests/unit/modules.test.js`, and `tests/integration/memberstack.integration.test.js` previously asserted "no inline `<style>` or `<script>` blocks anywhere" and "custom auth forms exist (`dpaLoginForm` / `dpaSignupForm`)". The landing page was redesigned to host its own scoped inline styles + a tiny `DefensivePositioningProAccess` shim that defers to Memberstack's hosted modals via `data-ms-modal="signup"` / `data-ms-modal="login"`. Tests now assert: (1) inline `<style>` / `<script>` only ever appear inside `#landingScreen`; (2) both Memberstack modal triggers and the `DefensivePositioningProAccess` shim exist; (3) `showDpaAuth` remains as a defensive no-op in `js/memberstack.js` for future re-enable.
    - Added 9 new tests: 8 in `tests/unit/modules.test.js` (L/R flip in both branches + perspective-neutral preservation + swipe handler bindings + direction assertion) and 1 in `tests/integration/ai-coach-brief.integration.test.js` (system-prompt language). Total suite: **260 tests passing (was 251 at Step 11)**.

### Phase 8 — Security Hardening

- [x] **Step 11** — Implement Security Hardening
  - Commit: `security: implement netlify and frontend hardening`
  - Notes: Added HSTS, Permissions-Policy, and Content-Security-Policy headers to `netlify.toml` (CSP includes `'unsafe-inline'` for script-src because index.html has 60+ inline onclick handlers that cannot be removed without a full refactor; CSP still meaningfully restricts external script sources, frames, objects, and base-uri). Added per-IP in-memory rate limiter to both Netlify functions (10 req/min for roster import, 30 req/min for AI brief) with IP extracted from x-forwarded-for header. Hardened `js/storage.js` load() with schema validation: rejects non-objects, deletes prototype pollution keys (__proto__, constructor, prototype), coerces each field to its expected type. AI responses already rendered via textContent (safe). Created `tests/integration/security.integration.test.js` with 34 new tests covering headers, secret isolation, rate limiting (static + functional), and storage safety. All 251 tests pass.

### Maintenance — Change Request: HBP button → Bunt (post-Step-11)

- [x] **Replace the HBP quick button with Bunt**
  - Commit: shipped in `feat: responsive field scaling, UI polish, and bunt outcome tests` (`cf7478c`)
  - What: "Hit By Pitch" was removed. The `recordOutcome('HBP')` quick button in `index.html` is now `recordOutcome('Bunt')` labeled "Bunt." Landing/feature/privacy copy that listed "HBP" now reads "bunts."
  - Tracking impact (investigated per request): A bunt is logged as a no-location plate-appearance event (the same `recordOutcome()` path Walk/HBP used), so it never has spray coordinates and cannot affect the spray chart or pull-tendency math (those require `e.x`). To keep the hot/cold (🔥/🧊) indicator, slump/hot-streak pills, AI coach-brief context, and the "Current: X for Y" at-bat count accurate, "Bunt" was added to every exclusion filter that previously listed "HBP": `js/recommendations.js` (lines 18/91/198), `js/ui.js` (hot/cold filter, AB-count filter, + comment), `js/ai.js` (coach-brief context). Net effect: a bunt is recorded and still shows in the Last 5 strip, but does not skew the live hitter read or the at-bat total — mirroring the prior Walk/HBP behavior. See `DECISIONS.md` #9 and `ARCHITECTURE.md` UI Conventions.
  - Tests: Added `tests/unit/bunt-outcome.test.js` (9 tests) — Bunt button wiring, full removal of "HBP" from active code (index.html + js modules), and a behavioral check that extracts `hitterStatus()` and proves a bunt does not corrupt the hot/cold read (a bunt between two cold ABs still reads 🧊, two bunts read neutral, a bunt between two hot ABs still reads 🔥, and Walk stays excluded). All 9 pass; full suite 289/289 green. The frozen `legacy/` snapshot was left untouched.
  - Adjacent cleanup (authorized): removed a dead inline `<script>` on line 25 of `index.html`. Commit `6022312` (PWA manifest) had rewritten the old Tesseract CDN `<script src=…>` into a bare inline `<script>` that only called the long-removed `renderSituationControls()` (deleted in Step 9, wrapped in try/catch). Being an inline script outside `#landingScreen`, it was failing `tests/unit/modules.test.js`; Tesseract is no longer used (roster import moved to OpenAI Vision in Step 8). Suite is now 289/289 green.
  - Adjacent E2E cleanup (authorized): `tests/e2e/modules.spec.js` "recommendation functions are defined" previously asserted `applySituationToDefense`/`ensureSituationState` are functions, but both were removed in Step 9. Dropped those two assertions (kept `renderSmartFielding`/`buildAIExplainText`/`toggleAIExplain`, which still exist). Verified: `npx playwright test tests/e2e/modules.spec.js -g "recommendation functions are defined"` → 1 passed.
  - Noted, not changed (out of scope): dead `#aiSituationModal` CSS remains in `styles/main.css` (~lines 1207–1211) from the same Step-9 situation-feature removal. Harmless (no matching HTML/JS); flagged for a future cleanup pass.

### Maintenance — Change Request: Responsive game-screen field sizing (post-Step-11)

- [x] **Fix the squished field on phones**
  - Commit: `fix: make game-screen field scale proportionally on mobile` (`403afa2`)
  - Symptom (reported from field use): on phones the field rendered as a flat sliver (~1/4 the screen or less) while the buttons looked normal-sized. Measured before the fix: field was **10.8% of screen, aspect ratio 4.81** at 360×640; **14.4% / 3.77** at 390×667 (iPhone SE); **18.3% / 2.86** at 414×736. Only large phones (390×844 → 28.8% / 1.49) looked acceptable.
  - Root cause: `#gameScreen .game-screen` is a fixed-height, no-scroll flex column whose only flexible child (`.field-wrap`) competes with **eight fixed control blocks** (game-top, mode-tabs, legend, tendency-pills above; compact batter strip, last-card, actions, run-dpa below — ~477px total). On shorter phones the field got whatever was left (~69–135px) and, because the field internals are percentage-positioned, that short box flattened the diamond. The container also used `100vh`, which ignores the mobile browser's address bar.
  - Fix (CSS-only, appended as a clearly-commented block at the end of `styles/main.css`):
    1. **Dynamic viewport height** — `#gameScreen.active` and `#gameScreen .game-screen` now use `height:calc(100dvh - 72px)` with a `100vh` fallback line first, so the layout tracks the visible viewport as the address bar shows/hides.
    2. **Proportional field min-height** — `#gameScreen .field-wrap` gets `min-height:clamp(150px, min(52vw, 46dvh), 320px)`. Driven by viewport width (bounds the diamond's aspect ratio) but capped by viewport height for landscape/short screens; flex-grow still lets it fill more on tall screens.
    3. **Scroll safety-valve** — `#gameScreen .game-screen` switched from `overflow:hidden` to `overflow-y:auto`. On screens too short to fit the bigger field plus every control, the lower controls scroll into view instead of crushing the field. Most phones (≈736px+ tall) still fit with no scroll.
    4. **Modest chrome compaction** under `@media (max-height:760px)` (tighter gap/padding, smaller Last-5 cells, trimmed action/run-dpa/batter-strip heights) to minimize scrolling — tap targets kept ≥ 50px.
  - Result (measured after): field is a consistent **~29–30% of the screen at aspect ratio ~1.5–1.8** across 360×640 / 390×667 / 414×736 / 390×844; 768×1024 tablet → 41% / 1.64. The diamond now renders properly (outfield arc, dirt infield, all nine positions spread, hitter card at home). Trade-off: on sub-~736px phones the bottom controls now require a small scroll (~30–85px) — documented in `DECISIONS.md` decision 10 and `ARCHITECTURE.md` UI Conventions. No JS or HTML changed; `index.html` and the JS modules were left untouched.
  - Tests: Added `tests/e2e/field-responsive.spec.js` (5 tests) — forces `#gameScreen` active directly (no auth/network dependency) and asserts, at four phone sizes, that the field aspect ratio stays < 2.2, the field holds > 25% of the viewport, the field still fills its width (no broken letterboxing), no horizontal overflow, plus that the container height tracks the viewport (`dvh`/`vh` calc). Verified the guard is meaningful: reverting the CSS fails it on exactly 360×640 / 390×667 / 414×736. All 289 vitest tests still pass (CSS-only change).
  - Note on E2E environment: the auth / Stripe / AI-coach-brief / gameplay E2E specs require the live Memberstack CDN (network) to reach protected screens; in an offline environment they fail by redirecting to the landing screen. Confirmed those failures are pre-existing/environmental (identical with the CSS reverted) and unrelated to this change. See `TESTING.md` Notes.

### Maintenance — Change Request: SVG field rebuild (post-Step-11)

- [x] **Rebuild the game field as a single SVG so the diamond stops distorting**
  - Commit: shipped in `feat: responsive field scaling, UI polish, and bunt outcome tests` (`cf7478c`)
  - Symptom (reported from field use): as the screen size changed, the field's white lines moved relative to the bases — the baseball diamond stopped looking like a diamond. Root cause: the field was ~20 independently-positioned `<div>`s using **three different coordinate conventions** (field-percentages for most elements, **fixed rotation angles** for the foul lines, fixed pixels for circles/bases) plus a `clip-path` dirt box sized on its own axes. They only aligned at one aspect ratio; once the responsive work let the field take different aspect ratios per device, the fixed-angle foul lines diverged from the percentage-positioned bases and the dirt diamond stretched independently.
  - Data finding: hits are stored as normalized `x`/`y` (0–100 of the field) and every calculation — pull side (`x<40`/`x>60`), depth (`y<38`/`y>62`), and fielder zone (point-in-polygon against the fixed dirt diamond `{50,39},{76,62},{50,84},{21,62}` in `js/utils.js`) — runs purely on those numbers, so the analytics were never corrupted. But the *drawing* didn't share that coordinate space, so the picture misrepresented the data, and capture was biased by the varying field shape across devices.
  - Fix (`index.html` + `styles/main.css`): replaced the background `<div>` stack inside `#field` (sky/outfield/foul/dirt/innergrass/lane/platecircle/mound/bases) with **one inline `<svg viewBox="0 0 100 100">`** drawn in the *same 0–100 space the data uses* — grass (gradient + mow stripes), outfield arc, foul lines run home→through 3B/1B→to the edges, infield dirt diamond, infield grass, home-plate dirt, mound, and the three bases + home plate. `#field` is locked to a **square aspect ratio** and centred in the wrap (wrap background is grass-coloured so the letterbox reads as outfield/foul grass). Position tags, the hitter card, hit markers, the tap-to-record handler, and swipe are **unchanged** — they sit on top of the SVG and stay aligned because they use the same 0–100 space. Position pucks/tags now scale with the field via container-query units (`cqmin`). Field `min-height` bumped to `clamp(190px, min(62vw,52dvh),340px)` to give the diamond room (the `#teamSprayScreen` export field is a separate screen and was intentionally left on the old markup).
  - Result (measured, forcing the game screen across sizes): **field aspect ratio = 1.000 on every viewport** (360×640 / 390×667 / 414×736 / 390×844 / 768×1024) — the diamond can no longer distort — and the SVG geometry point for first base maps to within **~0.5px** of where the data `%`-overlay places the same coordinate, i.e. the drawing and the data are pixel-aligned by construction. Field holds ~29–41% of the viewport; no horizontal overflow. Trade-off: the larger square diamond means the bottom controls scroll a little sooner on the shortest phones.
  - Tests: rewrote `tests/e2e/field-responsive.spec.js` (5 tests) for the new model — asserts the SVG `viewBox`, the locked square ratio (|AR−1|<0.06), **data/SVG pixel alignment** (first-base geometry vs the `%`-overlay, <1.5px), proportional size, no horizontal overflow, and the `dvh` container height. All 289 vitest tests and the 8 smoke E2E tests still pass; no JS referenced the removed background divs.

### Maintenance — Change Request: Field commands the space on one no-scroll screen (post-Step-11)

- [x] **Make the field large while keeping the whole game screen on one no-scroll page**
  - Commit: `fix: make game field the largest square that fits on one no-scroll screen` (`85fbd44`)
  - Symptom (reported from field use): on an iPhone 16 Pro the field rendered as a tiny square "about as wide as the Previous/Next button" while the rest of the UI looked fine. Measured before the fix (CSS logical px): field was **130×130 / 36% of the width** at 360×640, **148×148 / 40%** at 375×667, **272×272 / 69%** at 393–402-wide phones — and smaller still on the live device. Two firm requirements: (a) the field must command the space, and (b) the entire screen, top bar down to Run Analysis (with the Bible-verse footer fixed below), must fit with **no scrolling** — the coach must never scroll to reach a button.
  - Root cause: the prior "NO-SCROLL FLUID" design made `#gameScreen .field` `height:100%` of a `flex:1 1 0` `.field-wrap` inside a fixed-height column whose height was `calc(100dvh - 72px)`. The field, locked to a square, only ever got the vertical space the eight stacked controls left behind; that short leftover height made it a small square that ignored all the available width, and the `-72px` reserve plus the live mobile browser chrome shrank it further. (The earlier `field-responsive.spec.js` let this pass because it only required the field to be `> 10%` of the viewport height.)
  - Fix (CSS-only, edited the two authoritative blocks at the end of `styles/main.css`; no JS/HTML changed):
    1. **Largest square that fits** — `#gameScreen .field-wrap` is made a size container (`container-type:size`) and the field is sized `width:min(100cqw, 100cqh); aspect-ratio:1/1`. The field is therefore the largest square that fits the leftover space: it fills the **width** on tall phones and is capped by the **height** on short ones — always a true square, never taller than its space (so the screen never scrolls), never distorted.
    2. **Reclaim the whole viewport** — `#gameScreen.active`/`.game-screen` use `height:100dvh` (`100vh` fallback) instead of `calc(100dvh - 72px)`, and `body:has(#gameScreen.active) .app` zeroes the `.app` top/bottom padding (side padding kept so controls don't change width). `.game-screen` keeps `overflow:hidden` and a bottom padding (`clamp(22px,3.5dvh,38px)`) that lifts Run Analysis clear of the fixed verse footer.
    3. **No squish/spill** — every `.game-screen` direct child is pinned `flex-shrink:0` **except** `.field-wrap` (`flex:1 1 0`, the single flexible block), so controls keep their natural height and none spill into a neighbour (the compact batter strip previously overlapped the Last-5 card).
  - Result (measured after, CSS logical px; **verticalScroll = 0 and Run Analysis fully on-screen with 22–33px clearance on every size**, no overlaps): field is a square (AR 1.00) at **346² / 86% of the width on iPhone 16 Pro (402×874)**, 404² / 92% on the Pro Max (440×956), 330² / 84% at 393×852, and 268² / 67% at an address-bar-shown 402×760; on short phones it is height-capped to the largest square that still leaves every control visible — 209² (375×667), 192² (360×640), both up from ~130–148². The full diamond (outfield arc, dirt infield, all nine positions, hitter card) renders on one screen with the controls and verse all visible.
  - Note on device sizes: web layout uses **CSS logical pixels** (physical px ÷ devicePixelRatio), not the raw physical pixel count. iPhone 16 Pro is 1206×2622 physical at DPR 3 → **402×874 logical**.
  - Tests: rewrote `tests/e2e/field-responsive.spec.js` for the new contract at six phone sizes (incl. 402×874 and an address-bar-shown 402×760). Key guards: **no scroll** (`verticalScroll ≤ 1`), **no clip** (Run Analysis bottom ≤ viewport — the "don't make the user scroll to hit buttons" guard), the field **equals the largest square that fits** (`|fieldW − min(wrapW, wrapH)| < 3`), no control overlaps, locked square ratio, and data/SVG pixel alignment. (The old `> 0.1` height check that had let the collapse through is gone.) All 6 field-responsive tests pass; all 289 vitest tests and the 8 smoke E2E tests still pass. The frozen `legacy/` snapshot and `#teamSprayScreen` were left untouched.

### Maintenance — Change Request: Width-anchored proportional game-screen layout (post-Step-11)

- [x] **Field owns the full screen width; every control scales proportionally (no scroll)**
  - Commit: `fix: anchor game field to screen width with proportional controls`
  - Symptom (reported from field use): even after the prior "largest square that fits" work, on an iPhone 16 Pro (402×874) the field rendered ~298×298 (~34% of height) because it was sized from the *leftover* vertical space the controls left behind. The header, batter strip, Last-5 card, and action buttons stayed large and did not shrink with the screen, so the field was starved. Requirement: the field must be sized by the screen **width** — a full-bleed, edge-to-edge **square** (402×402 on a 402-wide phone) — and **every other row must divide the remaining height proportionally and scale its own text/buttons to fit, with no scrolling at any resolution.**
  - Root cause: `#gameScreen .field-wrap` was the sole flexible block (`flex:1`), so the square field only ever got `min(width, leftover-height)`; width was never the driver, and the controls' clamped heights left little behind.
  - Fix (CSS-only, `styles/main.css` consolidated block):
    1. **Field = width-anchored square.** `.field-wrap` is now `flex:0 0 auto`, `width:100vw` with negative side margins (`calc(50% - 50vw)`) so it runs **edge-to-edge** past the app padding, and `height:min(100vw, 92dvh)`; `.field` is `width:min(100vw, 92dvh); aspect-ratio:1/1`. The diamond is full screen width and square on every portrait phone (the `92dvh` cap only engages on freak short/landscape ratios so controls keep a sliver). Removed `container-type:size` from `.field-wrap` — with `flex:0 0 auto` it had been collapsing the wrap's basis to 0 (the field is no longer sized from the wrap's `cq` units, so the container is unneeded).
    2. **Every other row is proportional.** The eight control rows (`game-top`, `mode-tabs`, `legend`, `tendency-pills`, `#dpaCompactBatter`, `last-card`, `actions`, `run-dpa-row`) are `flex:1 1 0` with a per-row `flex-grow` **weight** (9 / 5 / 3 / 5 / 9 / 13 / 15 / 9) so they divide the leftover height in fixed proportion. Each row is its own `container-type:size` and its text/buttons are sized in `cqh` (1% of that row's height), with px only as `clamp()` readability rails — so a row's contents scale with the share it gets. No fixed pixel heights remain. This fixed the oversized "NYU Rockets" title and the Last-5 card / buttons that previously stayed large on short screens.
    3. **Edge-to-edge field, inset controls.** `body:has(#gameScreen.active) .app{ padding:0 }` drops the app padding so the field is truly edge-to-edge; `.game-screen` re-adds a thin side inset (`clamp(6px,2vw,12px)`) for the control rows only (the full-bleed field-wrap ignores it). Bottom reserve (`clamp(20px,3dvh,26px)`) keeps Run Analysis clear of the fixed verse footer.
  - Result (measured, seeded game): field is a full-bleed square — **402×402 (46% of H) at 402×874**, 402² (53%) at 402×760, 402² (62%) at 402×650, 360² (56%) at 360×640, 440² (46%) at 440×956 — with `field.left = 0` / `field.right = 0` (edge-to-edge) while the controls inset 8px each side. **verticalScroll = 0, no horizontal overflow, Run Analysis on-screen and clear of the verse, no control overlaps on every size.** Real phones share a ~2.2 aspect ratio, so the field lands at ~46% on all of them and the proportions stay consistent; only freak short ratios push it higher.
  - Tests: updated `tests/e2e/field-responsive.spec.js` to the new contract — the field width **equals the screen width** (`|fieldW − viewportW| < 2`, the edge-to-edge guard) alongside square ratio, no-scroll, no-clip, no-overlap, and data/SVG alignment at all six phone sizes; the proportional test now asserts the field is full-width 402 at both a tall (874) and short (640) height and that an action button measurably shrinks on the short one (catches any return to fixed-height controls). All 7 field-responsive E2E + 289 vitest tests pass. Supersedes the prior "largest square that fits the leftover" model. The frozen `legacy/` snapshot and `#teamSprayScreen` were left untouched.

### Maintenance — Change Request: Force AI Coach popup centering over JS inline styles (post-Step-11)

- [x] **Center `#aiExplainBox` on every screen, overriding the runtime inline styles**
  - Commit: `fix: force AI coach popup centering over inline styles` (`8eb731e`)
  - Root cause (found via DevTools on production): the popup opener writes **inline** `left: 8px; width: 414px; right: auto` onto the element at runtime (`element.style`, highest cascade priority). A plain stylesheet edit can't win against that, so the earlier non-`!important` centering fix had no effect on production. The inline `left:8` centers a 414px box for a no-margin world; the stylesheet's `margin-left/right: 18px` then displaced it ~18px right (and it overflowed the right edge by ~10px → the `right: -28` seen in the position diagram).
  - Fix (CSS-only, `styles/main.css` `.ai-explain-box`): made the centering declarations `!important` so the stylesheet beats the inline styles — `top:50% !important; left:50% !important; right:auto !important; transform:translate(-50%,-50%) !important; width:min(420px, calc(100% - 36px)) !important; margin:0 !important`. `left:50%` overrides inline `left:8px`; the responsive `width` overrides inline `width:414px` (fluid `calc(100% - 36px)` with an 18px gutter each side, capped at 420px on large screens); `margin:0` removes the displacing margins. Shared by `#aiExplainBox` and `#endGameModal` (`.end-game-modal` only overrides color).
  - Note: DevTools still *lists* `element.style { left:8px; width:414px; right:auto }` but now struck-through (overridden) — that strikethrough is the proof the `!important` override won.
  - Tests: regression test in `tests/unit/app.test.js` (CSS suite) asserts the rule contains the `!important` centering declarations and no `margin-left`/`margin-right`. Full vitest suite green (290).

### Phase 9 — Play-Log Import (Feature Request)

- [x] **Step 13** — Implement Play-Log Import
  - Commit: `feat: add play-log import`
  - Status: **Complete.** Spec in `PLAY_LOG_IMPORT.md`; design in `DECISIONS.md` decision 13.
  - Notes: Feature request — import play-by-play log screenshots into **previous-game** Defensive Positioning Assistant data. **Confirmed** the app's "main play object": each player carries `previousEvents`/`currentEvents` (balls in play with 0–100 `x`/`y`) and a unified `outcomes` log (also holds no-location Walk/Strikeout/Bunt). The importer writes into these existing arrays — **no new schema, no team-spray rebuild.**
  - What shipped:
    - **New Netlify Function** `netlify/functions/openai-play-log-import.js` — accepts an **array** of screenshots + `teamName` + `roster`, calls OpenAI Vision (`gpt-4o`, `detail:high`), and returns structured plays **for the coach's team only** (model identifies the batter, never a fielder; emits only confident matches; skips base-running outs). Per-IP rate limit 5/min, input validation, JSON-only sanitized output, no key exposure.
    - **New frontend module** `js/play-log-import.js` — pure helpers (`classifyPlayResult`, `canonicalOutcome`, `battedBallType`, `explicitDirection`/`fielderToPosition`, `playLogLocation`, `jitterCoord`, `matchBatterToRoster`, `dedupPlays`, `buildEventFromPlay`, `importPlayLogPlays`) plus DOM wiring (`openPlayLogImport`, `readPlayLogFiles`, `showPlayLogSummary`). Balls in play → `previousEvents` + `outcomes` (blue dot for hits/errors/FC, X for outs); walks/HBP/Catcher's Interference → `outcomes` as `"Walk"`; strikeouts → `outcomes` as `"Strikeout"` (keeps `kind`). Locations map to the **0–100** field space: ground balls → infield lane, line/fly → outfield, with ±2.5 jitter so dots don't stack. Reuses `preprocessRosterImage` for image prep.
    - **Matching:** jersey+name → jersey → fuzzy name (both "First L" and "F Last" forms). **Ambiguous or unknown batters are dropped** — no roster player is ever created, and there is **no "Unknown Player"** concept.
    - **Dedup** within an import (key = date + batter + result + normalized rawText); **multiple games per import** supported.
    - **UI:** `IMPORT PLAY LOG` button on the Team screen (`index.html` ~line 200), **disabled until the roster has ≥1 player** (toggled in `renderTeamScreen`, `js/ui.js`); multi-file picker; result summary modal (`#playLogSummaryModal`). CSS appended to `styles/main.css` (disabled-button style + summary rows). Load order updated in `index.html` and `CLAUDE.md` (`… → roster-import → play-log-import → memberstack → …`).
    - **No third-party brand name** appears anywhere in the shipped code/UI/docs.
  - Tests: `tests/unit/play-log-import.test.js` (51), `tests/integration/play-log-import.integration.test.js` (21), `tests/e2e/play-log-import.spec.js` (2 — button gating + mocked import writes data & shows summary). Full vitest suite **362 passing** (was 290). E2E runs locally (`npm run test:e2e`); the sandbox can't run Playwright.
  - **Import-status + summary-button polish (change request, same `feat: add play-log import` commit):**
    - **Per-button status lines.** Previously a single `#importStatus` div (under IMPORT PLAY LOG) was shared by both importers, so a roster-import result wrongly showed under the play-log button. Added a dedicated `#rosterImportStatus` div directly under IMPORT ROSTER (`index.html`) and pointed `js/roster-import.js` (paste/CSV success message + the AI-screenshot flow) at it. `#importStatus` now belongs solely to play-log, under IMPORT PLAY LOG. Each importer's status renders under its own button.
    - **Transient status.** `showScreen()` (`js/ui.js`) now clears both `#rosterImportStatus` and `#importStatus` on every navigation, so a stale message never appears when leaving and returning to the team screen. Import handlers update the screen via `renderTeamScreen()` (not `showScreen`), so a freshly-set message still shows while the coach stays on the page.
    - **Centered "Done" button.** The play-log summary modal's Done button was in a 2-col `.outcome-grid` (stuck in the left half). Replaced the wrapper with a centered flex container so the button is centered inside `.import-box`.
    - Tests: updated the three roster-import E2E assertions in `tests/e2e/roster-import.spec.js` to target `#rosterImportStatus`. Full vitest suite still **362 passing**.

### Maintenance — Change Request: Undo restricted to the current game (post-Step-13)

- [x] **Fix `undoLast()` removing previous-game data**
  - Commit: `fix: restrict undo to the current game`
  - Symptom (reported from field use): the Undo button could remove plays from Previous Games. Undo should only ever undo the current game.
  - Root cause: `undoLast()` (`js/game.js`) popped the newest entry from `player.outcomes` regardless of `mode`; when that entry was a previous-game hit (`mode === "previous"`) it also shifted `player.previousEvents`, deleting a historical spray marker. The unified `outcomes` log interleaves current and previous entries, so whichever was logged last was the one removed.
  - Fix (`js/game.js`): `undoLast()` now finds the most recent **current-mode** outcome (`outcomes.findIndex(o => o.mode === "current")`), splices that one out, and pops `currentEvents` only when the removed entry was a hit. `previousEvents` is never touched; if there is no current-game outcome, Undo is a no-op.
  - Tests: added `tests/unit/undo-last.test.js` (4 tests) — extracts `undoLast` with stubbed `save`/`renderGame`/`appData` and asserts: a current hit + its marker are removed; a newest previous-game hit is skipped and left intact while the current hit is undone (regression guard); a current no-ball undo leaves current hit markers intact; and Undo is a no-op when only previous-game outcomes exist. Full vitest suite **366 passing**.

### Maintenance — Change Request: Last 5 At Bats follows the game-mode toggle (post-Step-13)

- [x] **Make the Last 5 At Bats card track the Current Game / Previous Games toggle** (Spec 01)
  - Commit: _pending_
  - Spec: `specs/Completed/01-previous-games-last-5.md` (the new `specs/` folder mirrors the per-spec format used elsewhere; `specs/README.md` is the index).
  - Symptom: in Previous Games mode the field markers swapped to the previous-game blue dots (`renderEvents` was already mode-aware) but the **Last 5 At Bats** card still showed today's at-bats — `renderLast5()` (`js/ui.js`) hard-coded `o.mode === "current"`. The stats row had the same gap (`#currentStats` always "Current: X for Y", `#previousStats` always blank).
  - Fix (`js/ui.js` `renderLast5` only — no HTML/CSS/data-model change): the Last-5 selection and the stats totals (`abs`/`hits`/`outs`) now key off `appData.mode` instead of the literal `"current"`, mirroring `renderEvents`. In Previous mode the grid shows the 5 newest `mode:"previous"` at-bats and `#previousStats` reads `Previous: X for Y` (with `#currentStats` blanked); Current mode is unchanged. Walk/Bunt stay excluded from the at-bat count in both modes (DECISIONS.md #9). The 🔥/🧊 hot/cold icon (`hitterStatus`) is intentionally left current-game-only — it is not part of `.last-card`.
  - Tests: added `tests/unit/last5-mode.test.js` (6 tests) — extracts `renderLast5` via the `new Function` pattern with a minimal DOM stub (the suite runs in the `node` environment) and asserts: current mode unchanged (regression guard), previous mode shows previous at-bats + `Previous: X for Y`, a previous Walk shows in the grid but isn't counted, empty-cell padding under 5, the 5-most-recent cap, and outs counted per active mode. Full vitest suite **372 passing** (was 366).

### Maintenance — Change Request: Strikeout pitch type shows in Last 5 At Bats (post-Step-13)

- [x] **Show the pitch type on strikeouts logged in pitch mode** (Spec 02)
  - Commit: _pending_
  - Spec: `specs/Completed/02-strikeout-pitch-in-last5.md` (depends on Spec 01).
  - Symptom (reported from field use): with **Pitch: ON**, recording a Strikeout and picking a pitch type (e.g. Fastball) put the strikeout in Last 5 At Bats but with **no pitch tag** — the `<span class="ab-pitch">FB</span>` never appeared. The coach wants to see what pitch a batter struck out on, in both Current and Previous game views.
  - Root cause: the strikeout/walk pitch modal records through a deferred `pitchCaptureCallback` that reads `pendingPitch` to attach the pitch. But `selectPitch()` (`js/game.js`) called `clearPitchSelection()` — which sets `pendingPitch = null` — **one line before** invoking that callback, so `if(pendingPitch)` was always false and the pitch was never stored. The display (`renderLast5`, `js/ui.js`), the CSS (`#gameScreen .ab .ab-pitch`), and the previous-game carry-over (`confirmEndGame`'s `{...o, mode:"previous"}` spread, which preserves `pitch`) were all already correct — they just never received a stored pitch. The batted-ball path (`saveHit`) was unaffected; it reads `pendingPitch` directly before any clear.
  - Fix (`js/game.js` `selectPitch` only — no HTML/CSS/data-model change): removed the premature `clearPitchSelection()` call in the `if(pitchCaptureCallback)` branch. The callback now reads `pendingPitch` while it still holds the picked code (`FB`/`CU`/`CH`/`SL`/`OT`), stores it on the outcome, then nulls `pendingPitch` and calls `clearPitchSelection()` itself at the end (its cleanup was already there). **Skip** (`skipPitch()`) still records no pitch — it explicitly nulls `pendingPitch` before invoking the same callback. Walks route through the same modal path, so a walk logged with a pitch tags too (still excluded from the at-bat count per DECISIONS.md #9).
  - Tests: added `tests/unit/strikeout-pitch.test.js` (6 tests) — extracts `selectPitch`/`recordOutcome`/`skipPitch` from `js/game.js` via the `new Function` pattern (as `undo-last`/`last5-mode` do) and asserts: a Strikeout + picked pitch stores `pitch:"FB"` on the outcome (regression guard — fails on the old code), Skip stores no pitch, a Walk + pitch stores `pitch` and is not an out, `selectPitch`'s highlight branch still runs when no capture is pending, and `renderLast5` emits the `ab-pitch` tag for a stored pitch in **both** Current and Previous modes. Full vitest suite **378 passing** (was 372).

### Maintenance — Change Request: Play-Log Import fails at 7+ screenshots (post-Step-13)

- [x] **Fix the truncated-response failure when importing ~7+ play-log screenshots** (Spec 03, server-side fix only)
  - Commit: _pending_
  - Spec: `specs/03-play-log-import-many-screenshots.md` (steps 1–2 of the spec shipped; step 3, client-side batching, intentionally deferred — implement only if the 6 MB payload limit surfaces near the 12-image max).
  - Symptom (reported from field use): importing **7 or more** play-by-play log screenshots failed with "Import failed: Could not read plays from the screenshots. Try clearer images." Fewer screenshots worked; the images were fine.
  - Root cause: `netlify/functions/openai-play-log-import.js` requested the extracted plays with **`max_tokens: 4000`**. ~7+ screenshots' worth of plays overflowed 4000 output tokens, so OpenAI **truncated the JSON array mid-stream**; the function's hard `JSON.parse` threw and the `catch` returned 422 "…Try clearer images." There is no hard "7" limit in code — `MAX_FILES`/`MAX_IMAGES` are both 12; the 7 was the emergent token ceiling. (The 6 MB Netlify request-payload limit is a separate, higher ceiling that only bites near ~10–12 preprocessed images, so it was not the cause at 7.)
  - Fix (`netlify/functions/openai-play-log-import.js` only — no client, HTML, CSS, or data-model change):
    1. **Raised `max_tokens` 4000 → 16000** (gpt-4o supports up to 16,384 completion tokens), giving the plays array room.
    2. **Added `salvageObjects()`** — a brace-depth, string/escape-aware scanner (no `eval`, no dependency) that recovers the complete leading `{...}` objects from a truncated array. The parse `catch` now salvages instead of failing wholesale and only returns the 422 when **zero** objects are recoverable. Salvaged objects flow through the unchanged sanitizer.
  - Tests: added 3 tests to `tests/integration/play-log-import.integration.test.js` (now 24) — the request body carries `max_tokens >= 16000`; a truncated array (with a brace inside a string value, to prove string-awareness) yields a 200 keeping the complete objects and dropping the partial; a truncated response with no complete object still returns 422. Full vitest suite **381 passing** (was 378).

### Maintenance — Change Request: Public documentation reframed as an architecture case study (post-Step-13)

- [x] **Make the published documentation communicate architectural judgment, not just features**
  - Commit: _pending_
  - What: the repository read as "here is what I built and how to run it." Rewrote the public-facing docs so they lead with the problem, the constraints that drove the design, the decisions and the alternatives rejected, and measured outcomes.
    - **New `CASE_STUDY.md`** — executive summary; problem framing; the constraint table (dugout phone, ten-second budget, unreliable connectivity, verbatim relay, near-zero fixed cost); system architecture diagram; the central decision (**the rules engine decides, the model only phrases**) with the rejected alternative and four reasons; six decisions with alternatives rejected; the coordinate-space rebuild as *bug-class elimination*; the truncated-response salvage as *failure engineering*; unit economics; a measured-results table; an explicit **"what I deliberately did not measure"** section on the absence of on-field accuracy ground truth; lessons; and a table mapping every claim to the file or test that proves it.
    - **`README.md`** — restructured to a one-minute read (problem → architecture → outcomes table) that funnels to the case study, with the deep dives condensed rather than duplicated.
  - Accuracy: every figure in both documents was verified against the source before publication — 381 tests / 15 files (run), `RATE_MAX` 30/10/5, `max_tokens` 16000, `MAX_IMAGES` 12, seven distinct error status codes (400/405/413/422/429/500/502), `gpt-4o` vs `gpt-4o-mini`, 1-hour / 5-entry brief cache, zero runtime dependencies, ~300 KB client payload, ~4,200 test lines vs ~3,100 JS lines. Field-sizing and import-ceiling before/after numbers were taken from the measurements already recorded in this file. **No usage, adoption, or accuracy metrics were invented** — the evaluation gap is stated instead.
  - Doc corrections made in passing (defects found while verifying):
    - `DECISIONS.md` decision **7** stated "swipe right advances, swipe left goes back," which contradicts the shipped code (`js/batter.js` `setupSwipeNavigation`: `dx > 0` → `prevPlayer()`, `dx < 0` → `nextPlayer()`), `ARCHITECTURE.md`, and the tests. Corrected the row and the matching Standing Decision bullet to the implemented behaviour — a wrong standing decision would have led a future change to flip working code.
    - `DECISIONS.md` decision table: row **11** carried decision **10**'s content orphaned into extra columns, so the row rendered broken and decision 10 had no row of its own. Split into two well-formed rows and sorted the table by number (all 13 rows now have 5 cells).
    - `ARCHITECTURE.md`: Play-Log Import was still marked "planned" in three places although Step 13 shipped it; `play-log-import.js` and `openai-play-log-import.js` were missing from the repository-structure tree; the rate-limit note said "both functions" when there are three. Corrected, and added `CASE_STUDY.md` to the tree.
  - Tests: documentation-only change to published docs; no source, HTML, CSS, or data-model change. Full vitest suite re-run after the edits — **381 passing**, unchanged.
  - Noted, not changed (out of scope): `PROGRESS.md` is a ~50 KB internal build log that a reader arriving from `README.md` may open first; it is an accurate engineering record but not a reader-facing document. If the audience for this repository is external, consider linking readers explicitly to `CASE_STUDY.md` and leaving `PROGRESS.md` as the internal artifact it is.

### Maintenance — Change Request: Replace Mermaid blocks with purpose-built SVG figures (post-Step-13)

- [x] **Add four hand-built diagrams to the published documentation**
  - Commit: _pending_
  - What: added `docs/diagrams/` — four figures, each shipping a light and a dark file, swapped by `<picture>` + `prefers-color-scheme` so they follow the reader's GitHub theme. Replaced all three Mermaid blocks and added two figures that had no diagram before.
    | Figure | Placement |
    |---|---|
    | `architecture` | `README.md` "Architecture" **and** `CASE_STUDY.md` §3 — the two Mermaid blocks were near-duplicates; one canonical figure keeps them from drifting apart |
    | `model-boundary` | `CASE_STUDY.md` §4 (replaces the sequence diagram) |
    | `coordinate-space` | `CASE_STUDY.md` §6 (new) — the before/after that prose cannot carry |
    | `truncation-recovery` | `CASE_STUDY.md` §7 (new) |
  - Verified before publishing: no `<script>`, no external font/image/`href="http"` reference, no `foreignObject`, no event handlers — the figures are self-contained and safe to serve. All eight parse as well-formed XML and carry `<title>` + `<desc>`; each `<picture>` carries alt text. Text is real text (searchable, selectable, crisp at any zoom) in the system font stack, so GitHub renders it without loading a webfont. Confirmed the dark variants genuinely differ from the light ones. Scanned for client/team/player identifiers — none. All eight referenced paths resolve.
  - **Corrections made during review (a factual error, and it originated in our own docs):**
    - The architecture figure labelled roster import `gpt-4o · detail: low`. **The code uses `detail: 'high'` for both vision functions** (`openai-roster-import.js:102`, `openai-play-log-import.js:183`). The error traces back to the Step 8 note in this file ("uses `gpt-4o` with `detail: low` for cost efficiency"), which was carried into `CASE_STUDY.md` §5.2 without being checked against the source. Fixed in both SVGs and rewritten in §5.2 — the per-task cost argument now rests on model choice (`gpt-4o` vs `gpt-4o-mini`) and call frequency, which are true, rather than on an image-detail setting that is not. **The Step 8 note above is stale on this point; the code is authoritative.**
    - The figure's auth box read "gates the app; no game data ever leaves the browser." True of the auth provider, but unqualified it reads as a global claim that the same diagram contradicts one box over, where the finished recommendation flows to `ai-coach-brief`. Tightened to "gates the app; it never sees rosters or game data."
  - Everything else in all four figures was checked against source and is accurate: `RATE_MAX` 30/10/5, `MAX_IMAGES` 12, `max_tokens` 150 and 16000 (from 4000), the `×0.65` out weight, the current-vs-previous 4× contact weighting, and the field-sizing and alignment measurements.
  - Tests: documentation and asset change only; no source, HTML, CSS, or data-model change. Full vitest suite re-run — **381 passing**, unchanged.

### Final Validation

- [ ] **Step 12** — Create Full Regression Test Suite
  - Commit: `test: add comprehensive regression coverage`
  - Notes:

---

## Decisions Log

Any deviations from the playbook, resolved ambiguities, or scope decisions go here.

| Step | Decision | Reason |
|------|----------|--------|
| 1 | Created `index.html` at the repo root in addition to the `legacy/` snapshot. | `ARCHITECTURE.md` specified both `index.html` (live app) and a frozen `legacy/` snapshot; `index.html` is the file Steps 3–4 modularize. The legacy snapshot is not included in this published repository. |
| 1 | Git init/commit/push performed manually on Windows. | The agent sandbox's mount of the project folder cannot reliably maintain a `.git` directory; native Windows Git works correctly. |
| 3 | Replaced `serve` npm package with `scripts/serve.cjs` (zero-dependency Node.js HTTP server). | `serve@14.2.3` has a broken `chalk` sub-dependency on Node 22 (`ERR_MODULE_NOT_FOUND`). The custom server eliminates the external dependency, serves correct MIME types, and is guaranteed to work on any Node version. Removed `serve` from `package.json`. |
| 3 | Avoid literal `<style` in `.test.js` source files. | Vite's `import-analysis` plugin detects `<style` in JS source and misidentifies the file as HTML, refusing to parse it. Use `new RegExp('<' + 'style...')` in tests instead. |
| 4 | Excluded pitcher management blocks from modularization. | Pitchers were ruled out of scope for the app. Pitcher-related IIFEs (DefensivePositioningMyPitchers, DefensivePositioningPitcherMenu/Repair, Live Pitch Intelligence) were not extracted. Their HTML modal scaffolding remains in `index.html` but is inert without JS. |
| 4 | `zoneLabel`, `pct`, `shortOutcome` placed in `utils.js`. | These are pure functions used by both `ui.js` and `recommendations.js`; placing them in `utils.js` (loaded first) avoids cross-module load-order issues. |
| 4 | Pitcher files on disk but not referenced. | `js/pitchers.js` and `js/pitch-intelligence.js` were created before the "no pitchers" decision; sandbox permission prevented deletion. Not referenced in `index.html` — safe to delete manually. |
| 7 | Stripe billing handled entirely through Memberstack's native connection. | Memberstack has a built-in Stripe connection (`acct_REDACTED`). No standalone Stripe Netlify Functions needed. Removed stripe-create-checkout.js, stripe-webhook.js, js/stripe.js, and stripe npm dependency. |
| 13 | Play-log import writes into the existing previous-game data model (`previousEvents` + `outcomes`); v1 is import-only; unmatched plays are dropped (no "Unknown Player"); never creates a roster player; import button disabled until roster has ≥1 player; third-party brand name kept out of the product. | The existing blue-dot previous-game model already does what the feature needs, so writing into it is the smallest safe change and keeps imported data consistent with manual charting. Dropping unmatched plays keeps v1 the simplest possible import. See `DECISIONS.md` decision 13 and `PLAY_LOG_IMPORT.md`. |
